<div class="d-flex justify-content-between align-items-center row ">

  <div class="col">
    <ul class="navbar-nav ">
      <li class="nav-item">
        <form id="nav-signup" class="nav-link" method="GET" action="/borrowed">
          <button  class="btn btn-sm btn-outline-secondary " type="submit"><i class="fa-solid fa-folder">   collection </i> </button>   
        </form>
    </li>
  </div>
  <div class="col">
    <ul class="navbar-nav ">
      <li class="nav-item">
        <form id="nav-signup" class="nav-link" method="POST" action="/logout">
    <button  class="btn btn-outline-secondary " type="submit"> <i class="fa-solid fa-right-from-bracket">   logout</i></button>   
  </form>
  </li>
  </ul>
</div>
</div>
<div>
      <h3>Welcome</h3> 
    
       <h5><%=userBooks.currentUser.username %></h5>
       
      <div class="row row-cols-1 row-cols-sm-2 row-cols-md-3 g-4 ">
          <div class=" " >
            <div class="row ">  
              <% userBooks.allBooks.forEach(book => { %> 
            </div >
          </div>
          <div class="col-md-4 ">
            <div class="card  userProfile">
              <a 
              class="book-container"
              rel="noreferrer noopener"
            >
              <div class="book">
                <img class="userProfileImage img-fluid rounded-start"
                  alt="bookCover"
                  src="<%= book.image %>"
                  />
              </div>
            </a> <br>
              <div class="card-body">
                <div>
                  <h5> <%= book.title %></a></h5></div>
              </div>
              <div class="d-flex justify-content-between align-items-center">
                <div class="btn-group">
                  <form action="/book/<%= book._id %>">
                  
                  <button type="submit" class="btn btn-sm btn-outline-secondary fa-solid fa-eye"> view</button>

                </div>
                <a type="button" class="btn btn-sm btn-outline-secondary me-3 fa-solid fa-pen-to-square" href="/book/edit/<%=book._id %>"> Edit</a>
              </div>
              <% }) %>
            </div>
          </div>
        </div>
      </div>